# FONTE ÚNICA DOS GATES OBRIGATÓRIOS — workflow `ci-os-integracao.yml`.
#
# Este arquivo é lido por DOIS consumidores, e por nenhum outro caminho:
#   1. o passo "Publicar evidência na branch ci-evidencias";
#   2. `scripts/ci/portao_os_integracao.sh`, o agregador final.
#
# É PROIBIDO copiar esta relação para dentro do YAML. A divergência entre a
# lista da evidência e a lista do agregador foi exatamente o defeito CI-02:
# `rankingfn`, `rankingint`, `identint` e `auditident` apareciam na evidência,
# podiam falhar, e o portão final continuava verde porque não os percorria.
#
# Formato aceito pelo agregador: um gate por linha; `[A-Za-z0-9_]` apenas;
# linha em branco e linha iniciada por `#` são ignoradas; comentário no fim da
# linha NÃO é aceito (vira nome inválido e reprova a fonte inteira).
#
# Ausência de resultado, marcador `nao_<gate>`, exit code vazio, ilegível ou
# não numérico REPROVAM. Só `exit_<gate>` valendo exatamente `0` fica verde.
#
# `colevid` (gerador de PNG de evidência visual) NÃO entra aqui, por decisão
# registrada no cabeçalho do próprio teste: ele depende de fonte do sistema e
# não afirma regressão. Continua rodando e sendo reportado como informativo.

# --- 1. analyze -------------------------------------------------------------
analyze

# --- 2. suítes Flutter que já eram portão -----------------------------------
motor
resil
encerr
torneios
mtorneios
integr
colarte
colfire
colkit
social
casca
cascaaud

# --- 3. suítes Flutter de Mesa Online / Casca V2 (entram por esta OS) -------
# Verdes localmente desde a homologação da ligação motor↔Casca V2, mas sem
# gate próprio: podiam quebrar sem que o CI dissesse uma palavra.
cascavisao
cascamesaaud
cascav2
cascaligacao
cascamesa
cascaporta

# --- 3b. a Loja ligada à Casca V2 (entra pela OS da Ligação Produtiva) ------
# Prova as duas coisas que só um teste pode manter verdadeiras depois que a OS
# fechar: que existe caminho da Home publicável até a Loja (era este o buraco —
# o Billing do cliente estava completo e inalcançável, porque o único host
# morava no `main.dart` de prévias que a Casca V2 apagou), e que a Loja de
# produção não desenha carteira, pacote de moedas nem cosmético inventado.
#
# O caso `tocar em assinar NÃO acende o VIP` é o que impede a volta do defeito
# comercial: um `setState` local acendia o selo sem nenhum servidor envolvido.
cascaloja

# --- 3c. acessibilidade e segurança comercial da Loja (OS 14-C1) ------------
# A auditoria da OS 14 achou quatro P0, e DOIS deles não eram de leitura de
# tela — eram de cobrança: tocar num card de plano abria o fluxo de pagamento
# no mesmo gesto que selecionava, e o resultado da compra (recusa, erro,
# pendência) nunca chegava a ninguém.
#
# Nenhum dos dois era observável no aplicativo publicado, porque `master_vip`
# ainda não existe na Play Console e a vitrine vem vazia. Eles disparariam
# sozinhos, sem uma linha mudar, no dia da publicação — que é a única razão de
# esta suíte montar a vitrine COM planos.
#
# `lojaa11y` reprova se: selecionar voltar a comprar, a trava de intenção sair,
# o resultado deixar de chegar à tela, o anúncio de transição sumir, algum
# controle perder nome, algum alvo cair abaixo de 48 dp, a altura fixa voltar
# ao card de plano, ou o contraste do desconto voltar ao valor auditado.
lojaa11y

# --- 4. suítes Flutter de Ranking Real e Perfil Publicável (entram por esta OS)
# `rkbarreira` é a barreira temporal V2; `rkestado` guarda `EstadoRanking` como
# autoridade única (sem fallback Bronze/nível 1/#0/#1).
rkbarreira
rkestado
rkperfil
rkleitor
rkregressao

# --- 5. suíte da composição (entra por esta OS) -----------------------------
composicao

# --- 5b. domínio do CHAT LIVRE (entra pela OS do Chat Livre Seguro V1) ------
# A porta única de envio: conteúdo, superfície, assento, idempotência, filtro de
# bloqueio por par e a trava de projeção. É a suíte que reprova se alguém tirar o
# `avaliarContato` do caminho, aceitar autoria vinda do payload ou soltar o
# `messageId` derivado.
chatdom

# --- 6. teste do PRÓPRIO portão (entra por esta OS) -------------------------
# Sem ele, o agregador é a única peça do CI que ninguém verifica.
portaoci

# --- 7. codebases Firebase / Node que já eram portão ------------------------
billing
torneiosfn
socialdom
socialfn
socialemu

# [COMPOSICAO canonica] `regras` SAIU DAQUI, e nao por afrouxamento: o gate foi
# RENOMEADO para `colecoesemu` na OS do fail-closed — "nem o comando, nem a
# suite, nem o que ele prova mudaram", diz o comentario do proprio passo 3g do
# workflow. O nome velho ficou nesta fonte sem nunca ter tido produtor.
#
# O efeito era o contrario do CI-02, e igualmente ruim: em vez de gate que roda
# e nao e percorrido, um gate PERCORRIDO QUE NINGUEM RODA. O agregador falha
# fechado sobre ausencia de resultado — corretamente —, o que deixava o portao
# incapaz de ficar verde por um fantasma, e e assim que se cria pressao para
# afrouxar o agregador.
#
# A suite continua OBRIGATORIA sob o nome atual, `colecoesemu`, mais abaixo.
# PN-10 e PN-12 passam a exigir a bijecao nos DOIS sentidos, para que nem um
# gate sem produtor nem um produtor sem gate voltem a existir.

# --- 8. suite TRANSACIONAL do Passe VIP de Cortesia --------------------------
# O `rankingfn` prova a REGRA do passe (as duas fronteiras, o acumulo, o
# recibo) e roda sem emulador. Este prova o que so o banco prova: transacao,
# concorrencia (oito chamadas simultaneas => UM ciclo), persistencia campo a
# campo e falha fechada sobre documento corrompido.
#
# Ele entra como OBRIGATORIO, e nao informativo, pelo mesmo criterio do
# CI-02: uma suite que roda e nao e percorrida pelo agregador e uma suite que
# pode ficar vermelha sem ninguem saber. As Rules do bloco 8/8 ja entram pelo
# gate `colecoesemu`, que roda `test:integrado` (o gate chamou-se `regras`
# ate a OS do fail-closed; ver a lapide no bloco 7).
passeint

# --- 9. codebases que EXECUTAVAM e eram ignorados pelo agregador (CI-02) ----
# Estes quatro constavam da evidência desde a OS de integração de identidade,
# rodavam de verdade, e o portão final não os percorria. Agora são fatais.
rankingfn
rankingint
identint
auditident

# --- 9. codebase MODERAÇÃO (entra pela OS do Chat Livre Seguro V1) ----------
# LACUNA QUE ESTA OS FECHOU, e que não era do chat: `functions-moderacao` NÃO
# TINHA GATE NENHUM. A codebase que hospeda denúncia, bloqueio, sanção e
# `consultarContato` podia deixar de compilar, ou ver a barreira de idempotência
# quebrar, sem o CI dizer uma palavra — `npm test` dela existia e ninguém o
# chamava. `moderacaofn` roda `tsc` + os testes puros (idempotência e chat).
#
# `chatemu` é a integração REAL contra o Emulator Suite: autenticação, anônimo
# recusado, retry sob transação, concorrência, bloqueio nas duas direções e
# sanção. É o único gate que prova que a decisão do domínio chega ao Firestore.
moderacaofn
chatemu

# [COMPOSICAO canonica] Os dois gates de emulador que a linhagem de Billing/P0
# entregou como OBRIGATORIOS e que esta fonte ainda nao conhecia. Eles existem no
# workflow (produzem `exit_moderacaoemu` e `exit_colecoesemu`) desde aquela OS; o
# que faltava era a fonte unica saber deles — e um gate que o portao nao le pode
# falhar sem reprovar, que e o defeito CI-02 pelo outro lado.
moderacaoemu
colecoesemu

# --- 10. as TRES codebases que a composicao trouxe e o CI nunca conheceu -----
# `functions-conta`, `functions-mesas` e `functions-economia` sao implantaveis
# (estao em firebase.json, cada uma com codebase propria) e nao apareciam em
# passo nenhum do workflow. Nao e o CI-02 — aquele era gate que RODAVA e o
# agregador ignorava; este e o degrau anterior, codebase que nem chegava a
# rodar, e portanto sem nem evidencia. So a composicao podia enxergar isso:
# cada linhagem via apenas as codebases que ela mesma trouxe.
#
# `contaemu` e a exclusao de conta contra Firestore e Auth REAIS — e o unico
# lugar onde a etapa `mesas` da exclusao e de fato executada, e foi assim que a
# divergencia entre plano e execucao apareceu.
contafn
contaemu
mesasfn
economiafn

# --- 11. §16 proveniencia de SHA e §19 provas negativas da composicao -------
# `proveni` guarda o mecanismo que amarra o artefato implantado ao SHA da
# composicao: geracao a partir de `git rev-parse HEAD`, recusa de valor manual,
# verificacao byte a byte, e o `predeploy` que torna o passo impossivel de
# pular.
#
# `composneg` sao as 15 provas negativas da ARVORE COMPOSTA — invariantes que
# atravessam codebases e que nenhuma suite de folha pode enxergar sozinha. Ele
# le, entre outras coisas, ESTE arquivo e o workflow: e por isso que tirar um
# gate daqui derruba `composneg`, e nao so silencia o portao.
proveni
composneg

# --- 12. a COMPOSIÇÃO da Loja/Casca com as Functions canônicas -------------
# As duas folhas divergiram do MESMO ponto e nunca se viram. Cada uma tinha
# suíte própria e verde, e nenhuma das duas podia enxergar o que só existe
# depois da união — a começar por ela ter mesmo acontecido.
#
# `composloja` é o gate que reprova se QUALQUER UM DOS DOIS LADOS for removido:
# prova os dois SHAs de origem como ANCESTRAIS do HEAD (por isso o passo de
# checkout do workflow passou a ser fundo), que nenhum lado entrou só como
# documento, que as nove codebases e os 63 exports atravessaram inteiros, que
# os gates das duas folhas sobreviveram à união DESTE arquivo, e que a Loja,
# ao virar alcançável, não trouxe um segundo dono de sessão nem `.mock()` para
# dentro de `app/lib/casca/`.
#
# `rkpagina` é CI-01 encontrado outra vez, e no pior lugar possível: o único
# caso que monta `const RankingPage()` sem fonte — que é a construção REAL de
# `hall_page.dart` e de `perfil_page.dart` — morava em
# `test/ranking_page_test.dart`, e passo nenhum do workflow o rodava. A suíte
# que guarda a invariante RANKING-01 podia ficar vermelha sem ninguém saber.
rkpagina
composloja
