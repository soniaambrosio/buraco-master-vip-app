{
  "name": "bmv-social-functions",
  "private": true,
  "description": "Cloud Functions de identidade publica e grafo social: perfil, amizade e solicitacoes.",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "//build:domain": "O dominio Dart vira JS ANTES do tsc, que compila contra ele. Mesma ordem obrigatoria dos codebases de torneios e moderacao.",
    "build:domain": "cd .. && dart compile js -O2 -o functions-social/lib/domain_bundle.js app/lib/social/js_bridge.dart",
    "build": "tsc",
    "lint": "tsc --noEmit",
    "//test": "Compila primeiro porque o alvo do teste e a barreira JA compilada (lib/chaves.js). Nao precisa do domain_bundle: o teste nao carrega index.js, so o modulo puro de chaves e projecao. Alvos EXPLICITOS, e nao `node --test test/`: um arquivo novo na pasta passaria a rodar sem ninguem decidir, e um renomeado sairia da suite em silencio.",
    "test": "tsc && node --test test/chaves.test.js test/busca.test.js",
    "serve": "npm run build && firebase emulators:start --only functions:social",
    "deploy": "firebase deploy --only functions:social"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "typescript": "^5.6.0"
  }
}
